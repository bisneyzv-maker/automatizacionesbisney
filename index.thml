<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Automatizaciones Bisney | Soluciones con IA</title>
    <style>
        :root {
            --primary: #2563eb;
            --dark: #1e293b;
            --light: #f8fafc;
        }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--light);
            color: var(--dark);
            text-align: center;
        }
        header {
            background-color: var(--dark);
            color: white;
            padding: 3rem 1rem;
        }
        .container {
            max-width: 800px;
            margin: 2rem auto;
            padding: 0 1rem;
        }
        .btn {
            display: inline-block;
            background-color: var(--primary);
            color: white;
            padding: 0.8rem 1.5rem;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px;
            transition: background 0.3s;
        }
        .btn:hover {
            background-color: #1d4ed8;
        }
        .links-legales {
            margin-top: 3rem;
            font-size: 0.9rem;
            border-top: 1px solid #ccc;
            padding-top: 1rem;
        }
        .links-legales a {
            color: #64748b;
            margin: 0 10px;
        }
    </style>
</head>
<body>

    <header>
        <h1>Automatizaciones Bisney</h1>
        <p>Eficiencia y tecnología mediante Inteligencia Artificial y Automatización</p>
    </header>

    <div class="container">
        <h2>Nuestros Servicios</h2>
        <p>Transformamos procesos manuales en flujos automáticos optimizados.</p>
        
        <a href="#" class="btn">Ver Proyectos</a>
        <a href="https://t.me/TuUsuario" class="btn" style="background-color: #0088cc;">Contacto Telegram</a>

        <div class="links-legales">
            <p>Información Legal para Meta:</p>
            <a href="privacy.html">Política de Privacidad</a>
            <a href="conditions.html">Términos y Condiciones</a>
        </div>
    </div>

</body>
</html>
